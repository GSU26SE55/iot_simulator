# Copy thành .env rồi sửa theo môi trường của bạn:
#   cp env.example.txt .env
#
# Env LUÔN ưu tiên hơn config/seed.yaml.

# ─── Backend ────────────────────────────────────────────────────────────────────────────────
IOT_BASE_URL=http://localhost:4001
IOT_TLS_VERIFY=false

# `iot2-production` = contract mà firmware ESP32 thật chạy (đủ tính năng).
# `current`         = contract Sprint 1 cho backend đời cũ (tắt provision/MQTT/ambient/sự cố/OTA).
IOT_CONTRACT_VERSION=iot2-production

# API key mặc định cho thiết bị chưa khai trong seed.yaml.
# Backend chỉ trả `rawApiKey` MỘT LẦN lúc tạo device — chép ngay.
# ⚠ Key phải có scope tổng = 15 (SensorIngest|DeviceHeartbeat|EnvironmentalIngest|FirmwareCheck).
#   `EdgeDeviceDefault` mặc định = 11, THIẾU EnvironmentalIngest ⇒ ambient + sự cố trả 403.
IOT_API_KEY=iotk_replace_me_xxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── MQTT ───────────────────────────────────────────────────────────────────────────────────
# Bật để chạy đường MQTT-first.
# ⚠ Theo thiết kế `/provision` cấp đủ 6 trường lúc chạy, nhưng backend đang deploy CHƯA trả chúng
#   (đo 2026-08-11) ⇒ phải điền tay các giá trị dưới. Xem guideline.md §13.1–13.2.
# ⚠ Backend phải đặt `Mqtt__Enabled=true`, nếu không broker vẫn nhận publish mà không ai đọc
#   ⇒ mất telemetry hoàn toàn trong im lặng.
IOT_MQTT_ENABLED=false
IOT_MQTT_HOST=localhost
IOT_MQTT_PORT=1883
IOT_MQTT_TLS=false
IOT_MQTT_USERNAME=
IOT_MQTT_PASSWORD=

# ─── Hành vi simulator ──────────────────────────────────────────────────────────────────────
IOT_SEED_FILE=config/seed.yaml
IOT_QUEUE_DIR=logs/queue

# State bền vững — tương đương NVS của ESP32: provision, credential MQTT, bảng pin,
# version firmware đang chạy, máy trạng thái OTA.
IOT_STATE_DIR=logs/state
IOT_PERSIST_STATE=true

IOT_OTA_ENABLED=true
IOT_LOG_LEVEL=INFO
