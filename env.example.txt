# Copy thành .env và sửa giá trị theo môi trường của bạn:
#   cp env.example.txt .env

# Backend endpoints
IOT_BASE_URL=https://localhost:7200
IOT_TLS_VERIFY=false

# Default API key cho devices chưa override trong seed.yaml
# (devices tạo qua admin endpoint trả rawApiKey 1 lần — copy vào đây hoặc vào seed.yaml.devices[].api_key)
IOT_API_KEY=iotk_replace_me_xxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional MQTT (Sprint 4)
IOT_MQTT_ENABLED=false
IOT_MQTT_HOST=localhost
IOT_MQTT_PORT=1883
IOT_MQTT_TLS=false
IOT_MQTT_USERNAME=
IOT_MQTT_PASSWORD=

# Simulator behavior
IOT_SEED_FILE=config/seed.yaml
IOT_QUEUE_DIR=logs/queue
IOT_LOG_LEVEL=INFO
